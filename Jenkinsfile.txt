pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                // Clean workspace and fetch code from Git repository
                checkout scm
            }
        }
}